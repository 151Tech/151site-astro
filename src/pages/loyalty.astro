---
// The standalone loyalty policy page was replaced by two entries in the
// homepage FAQ (see src/pages/index.astro, loyaltyFaqItems) with friendlier
// wording. This redirect keeps any existing inbound links/search listings
// pointed at the FAQ instead of 404ing.
return Astro.redirect('/#faq', 301);
---
